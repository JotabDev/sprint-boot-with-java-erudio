#Inicialização para teste

server:
  port: 8888

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  #Sera gerado dinamicamente em runtime
  #  url: jdbc:mysql://localhost:3306/rest-jdbc-boot?useTimezone=true&serverTimezone=UTC
  #  username: root
  #  password: 123456
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
   # properties:
     # hibernate:
      #  dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
springdoc:
  pathsToMatch: /api/**/v1/**
  swagger-ui:
    use-root-path: true
      